export type BrowseCommand = "doctor" | "status";

export function describeBrowseRuntime(): string {
  return "codex-stack browse is scaffolded. Add Playwright-backed commands in the next milestone.";
}
